// Previously On - Webview client script (standalone variant)
// The main logic is inlined in summaryWebviewPanel.ts getHtml() for M1 simplicity.
// This placeholder mirrors ARCHITECTURE.md §6 file tree.
(function () {
  const vscode = acquireVsCodeApi ? acquireVsCodeApi() : undefined;
  if (!vscode) return;
  // Reserved for future M2+ interactivity
})();
